export type ProductT = {
    name: string 
    route: string
    color: Array<string>
    price: number
    sizes: Array<number>
    images: Array<string>
    description: string 
}

export type MessageT = {
    names: string
    email: string 
    message: string
}